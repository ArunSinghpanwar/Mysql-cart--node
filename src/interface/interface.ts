export interface login {
    Gmail: String,
    Password: Number
}

export interface Register {
    first_name: String,
    Last_name: String,
    Age: Number,
    Gmail: String,
    Password: Number
}

export interface Cart {
    id: Number,
    Product_id: Number
}
